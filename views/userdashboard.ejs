 <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f2f2f2;
        }
        
        .dashboard {
            background-color: #fff;
            width: 80%;
            margin: 20px auto;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1 {
            text-align: center;
        }

        .actions {
            display: flex;
            justify-content: space-between;
        }

        .action {
            width: 30%;
            padding: 20px;
            background-color: #3498db;
            color: #fff;
            text-align: center;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .action:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>
   
    <div class="dashboard">
        
        <div>
            <h1> Hello <%= name %>
            </h1>
        </div>
        <div class="actions">
            <div class="action" id="pay-fine">
                Pay Fine
            </div>
            <div class="action" id="return-book">
                Return Book
            </div>
            <div class="action" id="request-book">
                Request Book
            </div>
        </div>
    </div>

    <div>
      <h1>Books</h1>
    </div>

    <script>
        // JavaScript code to handle actions
        document.getElementById('pay-fine').addEventListener('click', function () {
            // Add your code to handle paying fines here
            alert('Fine paid successfully!');
        });

        document.getElementById('return-book').addEventListener('click', function () {
            // Add your code to handle returning books here
            alert('Book returned successfully!');
        });

        document.getElementById('request-book').addEventListener('click', function () {
            // Add your code to handle requesting books here
            alert('Book request sent!');
        });
    </script>
</body>
</html> 
